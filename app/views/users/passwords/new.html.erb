<br>
<div class="container p-3 rounded-3 shadow-box" style="margin: auto; text-align: center">
  <h2>Forgot your password?</h2>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "users/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", style: "width: 50%; margin: auto" %>
    </div>

    <div class="actions">
      <%= f.submit "Send me reset password instructions", class: "btn btn-dark" %>
    </div>
  <% end %>
</div>
<div style="margin: auto; text-align: center">
  <br><br>
  <%= render "users/shared/links" %>
</div>